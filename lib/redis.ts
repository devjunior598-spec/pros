/**
 * lib/redis.ts
 * Redis publisher stub to satisfy Turbopack bundle resolution.
 * If Redis is configured, replace this with a real ioredis or redis client instance.
 */
export const redisPublisher: any = null;
